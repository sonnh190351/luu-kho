export enum DatabaseTables {
    Auth = "auth",
    Tags = "tags",
    Inventories = "inventories",
    Items = "items",
    Warehouses = "warehouses",
    Categories = "categories",
    Suppliers = "suppliers",
    UserDetails = "users",
    Users = "users",
    Requests = "requests",
}
